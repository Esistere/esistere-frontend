import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.paolo',
  appName: 'paolo',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
